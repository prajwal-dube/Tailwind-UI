/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["*"], 
  theme: {
    extend: {},
  },
  plugins: [],   
}

// In content array we have to add 
// the path of all the files where 
// we are going to use tailwind css 
// classes
// If we want to use in all the files